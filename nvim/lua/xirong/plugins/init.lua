
-- Plugins that doesn't need configuration, so add their link directly -- 

return {
    "christoomey/vim-tmux-navigator"    -- navigate through using tmux style
                                        -- ctrl+hjkl already set within the plygins
}
